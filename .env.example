# Copiá este archivo a `.env.local` y completá la clave (no subas `.env.local` a git).
VITE_GEMINI_API_KEY=AIzaSyBfuL_ohIDY4onZCsryli1QP48yHPa9z8Q
# Opcional — ver https://ai.google.dev/gemini-api/docs/models
# Por defecto: gemini-2.5-flash-lite (más barato / más cupo en free tier que 2.0-flash).
# VITE_GEMINI_MODEL=gemini-2.5-flash
# OpenAI/Claude desde el navegador suelen bloquearse por CORS; hace falta un proxy backend si migrás.
